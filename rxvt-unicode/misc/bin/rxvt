#! /bin/sh
URXVTD="urxvtd -q -f"

if ! pgrep -u $USER urxvtd > /dev/null
then
	if [[ $SSH_AUTH_SOCK ]]
	then
		$URXVTD
	else
		ssh-agent $URXVTD
	fi
fi

exec urxvtc -ls -vb -fn 'xft:Bitstream Vera Sans Mono:' -fb 'xft:Bitstream Vera Sans Mono:style=Bold' \
	"$@"
